{
  "name": "fluxstack-library",
  "version": "0.2.0",
  "private": true,
  "description": "Visual AI tools directory with Vercel serverless API and Postgres persistence",
  "type": "module",
  "scripts": {
    "dev": "vercel dev",
    "check": "node --check app.js && node --check lib/db.js && node --check 'api/[...route].js'"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "pg": "^8.13.1"
  }
}
